import React from 'react';

interface AddLinkProps extends React.AnchorHTMLAttributes<HTMLAnchorElement> {}

/**
 * Represents a link with add icon.
 *
 * Base props are extended from base `<a />` component.
 */
export function AddLink(props: AddLinkProps) {
  return <div />;
}
