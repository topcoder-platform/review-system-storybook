import React from 'react';

interface IconChainProps {
  /**
   * A list of icons.
   */
  children: React.ReactNode;
}

/**
 * Represents a icon chain component.
 */
export function IconChain(props: IconChainProps) {
  return <div />;
}
