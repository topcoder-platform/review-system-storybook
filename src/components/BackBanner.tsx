import React from 'react';

interface BackBannerProps {
  /**
   * The content.
   */
  children: React.ReactNode;
  /**
   * The url to navigate.
   */
  url: string;
}

/**
 * Represents a Back Banner component.
 */
export function BackBanner(props: BackBannerProps) {
  return <div />;
}
