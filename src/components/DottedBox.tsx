import React from 'react';

interface DottedBoxProps {
  /**
   * The content.
   */
  children: React.ReactNode;
}

/**
 * Represents a dotted box component.
 */
export function DottedBox(props: DottedBoxProps) {
  return <div />;
}
